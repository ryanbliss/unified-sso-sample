[
    {
        "name": "CreateNote",
        "description": "Creates a note",
        "parameters": {
            "type": "object",
            "properties": {
                "text": {
                    "type": "string",
                    "description": "The text of the note the user will create"
                }
            },
            "required": [
                "text"
            ]
        }
    },
    {
        "name": "GetNotes",
        "description": "Retrieve a list of the users notes"
    },
    {
        "name": "SuggestEdits",
        "description": "Suggest an improvement for the note the user is currently editing"
    },
    {
        "name": "GetMember",
        "description": "Gets a member by id",
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string",
                    "description": "The unique UUID of a user"
                }
            },
            "required": [
                "text"
            ]
        }
    }
]